# HuntWarden 版本发布说明

本文合并各版本面向用户的发布摘要。完整逐项变更仍以根目录 [`CHANGELOG.md`](../CHANGELOG.md) 为准；当前支持边界以 [`支持与验收说明.md`](支持与验收说明.md) 为准。

## 未发布复验（2026-09-14）

真实 Provider 联合资格已在干净合并提交 `f804591ebff84cba4e4aaad4b9d8ffb21debd203` 重新执行并得到 schema v2 `PASS`。目标为 Ubuntu 24.04 ARM64 真 VM 和 Docker Tomcat 9/JDK 17，Provider 为远端 `tokenrhythm/glm-5.3-flash`；凭据由 macOS Keychain 受控桥接，仅向固定 Provider 子进程注入，运行结果记录 `plaintextPersisted: false`。

- Linux 零 IOC：Task/Epoch `COMPLETED`，调查 `CLOSED_NO_OBSERVED_FINDING`；12/12 次 Provider HTTP 成功，2 个 MODEL Action 成功，15 个 DiscoveryCheckpoint 完整，报告 1 份。
- Java 零 IOC：Task/Epoch `COMPLETED`，调查 `CLOSED_NO_OBSERVED_FINDING`；21/21 次 Provider HTTP 成功，3 个 MODEL Action 成功，17 条类字节码 Evidence 完整，其中 5 条由模型引用，报告 1 份。
- 资格生成器复核了前后提交与干净工作树、Helper SHA-256 `fb590c98547455ce8e9b2142127afa7809c3492bc8f5943ef3cae966278d362f`、在线 Tool Call、稳定公网 DNS、故障降级契约、无重复原语执行和零非法 Tool Call。机器原件位于 `~/HuntWarden-acceptance-20260914-provider/release-first/provider-real.json`；之前的失败与受限运行保留在同一外部验收根，不改写为本次通过。

此 PASS 只适用于 `f804591e` 的真实 Provider 单项。已发布 `v0.3.0-beta.2` 资产仍绑定 `5d416ae7`；五平台、真实业务 JVM 与运维 PASS 原件绑定更早候选，尚未在 `f804591e` 重跑；独立冻结的 100 恶意 + 100 良性 + 受限盲测仍未执行。因此不能把本次结果改绑到现有资产，也不能提升为稳定发布，整体仍为 **BLOCKED**。

## v0.3.0-beta.2（2026-09-14，预发布）

本次交付为单维护者委托的隔离环境验收，不是独立第三方验证。执行者如实记录为 `AI/automation`，责任范围仅限维护者自有部署；没有要求或伪造上游、生产用户背书。当前结论为 **预发布已交付，稳定发布 BLOCKED**。

### 变化

- 增加无答案调查批跑、FIRST/RETRY 账本、固定真值归档和数据库 Epoch 起止身份核验；开发记录不能升级为独立盲测资格。
- 五个平台使用真实 VM、官方镜像/摘要与本次带外 Host Key；业务 JVM 支持嵌入式 ROOT Context、真实 ClassLoader，以及 HTTP 200 内业务错误识别和同并发负载比较。
- 修复桌面崩溃恢复时启动等待任务结束的死锁；根目录过多时遵守 `limitRoots`，保留截断 Coverage 而不是虚报完成。

### 本次实测

| 项目 | 结果与边界 |
| --- | --- |
| 代码与界面 | 244 项测试通过；Docker 14/14、GUI E2E 16/16，保留首次失败日志；类型检查、构建、lint、Probe 和 RE2 已通过 |
| 平台 | Ubuntu 24.04 ARM64/x86_64、Debian 12 systemd x86_64、Rocky 9.8 x86_64 Enforcing、Amazon Linux 2023 x86_64，正式执行器 5/5 PASS；四个 x86_64 为 QEMU/TCG，非原生性能结论 |
| 真实业务 JVM | RuoYi-Vue 原版隔离业务，8 路并发、2,497 次负载请求零失败；12 次 Attach，必需组件/成本/身份通过；基线/负载 P95 47/22 ms，Attach 最大 504 ms |
| 运维 | 同提交安装/升级、权限/摘要、回执保留、两种卸载、真实旧库 schema 5→6 与回退、两个 Artifact 离线导出 PASS |
| 规模 | 1001/10001 进程、100001 文件、100000 条活动/轮转 gzip 日志通过；1 万进程控制端首 Evidence 约 648 ms、早于末页；不外推跨平台 SLO |
| 打包应用 | 实际启动版本 0.3.0-beta.2，配置页、新建任务表单、返回首页交互通过；未声称用该 GUI 完成真实目标全流程 |
| 真实 Provider | 本机协议契约通过；FIRST 失败，单列 RETRY 的 18 次 HTTP 全部 200，但 Linux 采集缺口导致 COMPLETED/PARTIAL/LIMITED，联合资格仍 FAIL |
| 独立盲测 | 未执行；尚缺独立冻结和隔离的 100 恶意 + 100 良性 + 受限总体。开发单例与重跑不能代替 |

### 验收候选与发布身份

- 资格演练候选提交：`14913025fbac44ecbef6e21e77a1923f6b609b21`；Helper SHA-256：`7bdfb26aa60a6ed8a3383a94e32f85d3851a1769749cda27946cf541a75a161a`。
- 本地原件根：`~/HuntWarden-acceptance-20260913-maintainer/`，包含授权、源码/环境/输入冻结、首跑和重跑证据、日志及截图；敏感运行库和凭据配置不随公开资产分发。
- GitHub Prerelease [`v0.3.0-beta.2`](https://github.com/IceWindy233/HuntWarden/releases/tag/v0.3.0-beta.2) 从 PR #8 合并提交 `5d416ae70a7e220ad298e47d1ca2da8072e7dc20` 重新构建。发布资产中的 `COMPONENTS.json` 绑定该提交；ZIP SHA-256 为 `df182a63d1cc5e6c7f9bd5559ea9443c8619592334df022675b2b875a9a40e15`，DMG SHA-256 为 `44afec3ae344c748a973b8171c8ebd9959719e64cc052c2acb73cdf910ae4ae4`。
- 远程 Release 同时提供 ZIP、DMG、`COMPONENTS.json`、`PRERELEASE-NOTICE.txt` 和 `SHA256SUMS`；GitHub 记录的五份资产摘要与本地清单一致。macOS ARM64 资产未签名、未公证。
- `14913025` 上的平台、业务 JVM 与运维 PASS 仍只是资格演练原件，不能改绑到发布提交 `5d416ae7`。FIRST、失败与未执行项全部保留；预发布通道不放宽稳定门禁。

Java 补充诊断另以 `DEVELOPMENT` 单例执行：12 次 Provider HTTP 全部 200，Task/Epoch COMPLETED、调查 CLOSED_NO_OBSERVED_FINDING，候选起止身份一致。它只证明本次 Java 路径能闭合，不替代失败的联合资格或独立盲测。汇总执行器已生成 `qualification.BLOCKED.json`，明确缺少 `provider` 和 `blindEvaluation` 正式原件。

## v0.3.0-beta.1（2026-09-10，预发布）

自主深度取证主链已经按 Manifest/Helper `3.0.0` 实现：易失优先发现、持久化调查状态机、增量规则与流程包、结构化模型提案、自动分页、Evidence 保全、独立完成校验、GUI/报告投影和端到端 evaluator 均已接入生产运行时。

当前工程验收包括控制端 4 恶意 + 4 良性 + 4 受限场景、两种 OpenAI-compatible 协议的本机 HTTP/SSE 契约 1/1、Docker 五类 Lab 14/14、Debian 动态 SSH 6/6、Ubuntu 24.04 ARM64 smoke 4/4、journald 1/1 与 systemd transient unit 1/1、GUI 16/16、RE2 双环境和 Tomcat 9/JDK 17 真探针。协议契约实际核对请求、工具 Schema、流式 Tool Call 分片重组、完成原因和 usage；故障矩阵完成 429→200 逐次审计、105 ms SSE 停滞中止和空响应拒绝，但不替代真实厂商 Provider。systemd 专项验证运行态 transient fragment、active/命令/执行文件关系，并在清理后带外核对为 `LoadState=not-found`。Docker Tomcat 部署固定摘要的 Spring MVC 5.3.39，实际发现 `/spring/health` Controller、Interceptor 和 `/ws/{id}` WebSocket endpoint，并在静态页/Spring Controller 混合的持续 8 路 HTTP 流量下完成 12 次连续受控 Attach：1,758 次请求零失败，负载 P95 2 ms、最大 5 ms，Attach 最慢 82 ms，动态 Filter、WebSocket、Spring Controller 与 Interceptor 均无漏检，原始 JVM identity 保持稳定且每次均有可审计成本；1001/10001 个真实进程、100001 个文件与 100000 条轮转/gzip 日志的生产 Helper 分页已在 Ubuntu 22.04 ARM64 容器通过。生产控制端规模链在 1 千/1 万进程下均于末页前保全首屏异常执行体，完整耗时 4.078/22.627 秒，首 Evidence 593/590 ms，Observation 提交至 Evidence 为 216/221 ms，50 ms 采样最大 RSS 195.4/673.5 MiB；动态 SSH 场景还以 256 KiB 固定分块完成 100 MiB Evidence 的端到端字节数和 SHA-256 核对。空 assistant 会保留原始轨迹并将模型审查降为 LIMITED；队列容量、自动翻页、Apache/PHP-FPM、SSH Match、wtmp/btmp、systemd drop-in 和类字节码 Evidence 已纳入回归。

发布资格已提供可执行生成路径：真实 Provider 结果来自在线冒烟、两个完整 RuntimeStore 任务与协议故障契约；真实业务 JVM 在冻结业务流量下连续 Attach；平台结果由 Helper 观测发行版、PID 1 与 SELinux，并实际完成 Evidence 传输、清理和 SSH 重连。运维资格另在可销毁 Linux 环境实跑安装、升级和卸载，再由控制端验证旧 schema 事务前备份、迁移、旧任务读取、回退、重新迁移及脱敏 Evidence 离线导出。所有结果绑定当前提交及适用的 Helper/脚本摘要；发布校验器逐字段核验，手写模板不能通过。

该版本仅作为 GitHub Prerelease 发布，不是稳定版或完整发布资格结论。真实 Provider 联合资格已在早期固定提交通过；仍缺绑定本标签的至少 100 恶意 + 100 良性独立盲测、非夹具真实业务 Attach、其余四个平台和正式运维证据。资产内含 `PRERELEASE-NOTICE.txt`，达到全部门禁前不得改标为稳定版。

## v0.2.0（2026-09-01）

v0.2.0 将生产调查链路完整切换到 Tool Protocol v2。新任务只运行 v2 Epoch；模型使用八个受限取证原语，Helper Observation 经 Manifest 校验后原子写入 Fact Store，再由本地查询、Coverage 和 RULE/MODEL/HUMAN Assessment 独立呈现。

### 主要变化

- Manifest/Helper 升级到 `2.1.0`，补充委派规则、SSH 有效信任配置、固定 file Scope 与 `package_db` 有界验证链路。
- 删除运行时 v1 operation map、旧问题型工具面和旧 Finding 结论平面；v0.1.x 历史任务仅保留只读元数据、Evidence 和已有报告。
- 五类检测迁移为版本化 Preset、CoverageRun 与不可互相覆盖的 Assessment。
- Object、Cursor、Query、Evidence、Grant、Budget 与 Action Receipt 全部绑定 task、epoch 和目标身份；不兼容引用与越权请求失败关闭。
- 增加 Fact Store、Dataset Registry、RE2 双平台矩阵、v1→v2 能力等价门禁、四套 GUI E2E、真实 VM/journald 验收和冻结模型评测。
- 默认处置白名单只开放 `quarantine_file`；`disable_account` 因信任面和恢复链路不完整而保持关闭。

### 验证与限制

- PR、构建和静态门禁通过；Docker Lab 11/11、Debian 动态场景 5/5、GUI E2E 16/16。
- Ubuntu 24.04 ARM64 上完成 smoke 4/4、journald 1/1、模型评测 7/7，以及五类 × QUICK/STANDARD/DEEP 的 15 个正式任务。
- Provider 首跑出现过一次空响应和两次非法引用，控制端均按预期处理，因此综合结论为 `PASS_WITH_LIMITATIONS`。
- 这是破坏性展示升级：v0.1.x Finding 不再由 v2 UI/TUI/报告读取，但数据库行不会被删除。
- 目标端必须使用匹配的 Helper `2.1.0`；恢复动作、更多发行版与 JVM 版本仍待实现或验收。

## v0.1.1（2026-08-21）

v0.1.1 是 Electron GUI 补丁版本，不改变安全边界、Helper 协议、模型调用方式或任务数据格式。

- 修复任务侧栏和长详情内容无法滚动的问题，并补充语义化区域与无障碍标签。
- 修正 Grid/Flex 最小高度约束，使固定窗口内形成真实溢出区域。
- 发布脚本只收集版本号精确匹配的 ZIP/DMG，避免历史构建产物混入。
- 自动化测试 131 项通过、34 项跳过，GUI 调查 E2E 4/4 通过。

## v0.1.0（2026-08-20）

首个公开版本提供五类 Linux 主机专项检测、可追溯 Evidence/Finding、逐动作审批、崩溃恢复和版本化 Markdown 报告。

### 核心能力

- WebShell、Tomcat 9/JDK 17 Java 内存马、后门账户、Linux 持久化与 Linux 入侵分诊。
- 安恒威胁情报只富化当前任务内已经观测或由分析师预置的 IOC。
- SSH Host Key 严格绑定；Helper 只接受固定操作和 JSON 参数，不提供任意命令执行。
- `SCAN` 没有写工具；`REMEDIATE` 的每个动作均需一次性授权。
- 原始 Evidence 不进入模型上下文，采集缺口不会被描述为安全。

### 历史限制

v0.1.0 仅支持单台 Linux 主机直连 SSH；缺少 root、YARA、日志或 JDK Attach 时能力会降级；macOS arm64 开发包未签名或公证。该版本的 V1 验收结果已经被 v0.2.0 的 V2 基线取代，不作为当前支持证据。

## 安装与校验

从对应 GitHub Release 下载 `.zip` 或 `.dmg` 以及 `SHA256SUMS`，在 macOS 上执行：

```bash
shasum -a 256 -c SHA256SUMS
```

从源码复现时切换到对应 tag，再执行：

```bash
npm ci
npm run build
npm test
npm run probe:build
```

发布资产必须从最终 tag 对应的干净工作树生成。开发包尚未使用 Apple Developer ID 签名或公证；只调查你拥有或明确获授权的主机。
